# Reliability Design — Unit: docs-navigation

> **N/A** — ui kind; no reliability-design produce (`produces_kinds` = service). Stub for consume chain (Q5 = A).

## Status

Not applicable as a service SLO design. Failure modes (invalid payload → ignore; unmapped → Shell top; no silent open-doc fallback) live in [security-design.md](./security-design.md) and the business-logic model.

## Review

**Verdict:** READY (N/A)
