**Collaborator:** aidlc-devsecops-agent

## Contribution

Supply-chain / hygiene for Bolt 4. No new cloud deps. Continue Biome + `bun audit` inside `bun run check`. VSIX must remain free of secrets, `.env`, and `aidlc/` runtime (Q3 = A). No new Mandated/Forbidden beyond existing project Forbidden (Q4 = A).

## Positions

AGREE: Q3 = A, Q4 = A; local-only deployment unchanged.  
OBJECT: None
