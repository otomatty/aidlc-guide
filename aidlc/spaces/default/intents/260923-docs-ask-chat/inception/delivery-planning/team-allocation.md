# 担当

担当（mob）は、その通しを作る側である。チーム編成の段は、この流れでは行っていない。通しは一つで、すべて `aidlc-developer-agent` が作る。

## 割り当て

| 通し | 塊 | 担当 |
| --- | --- | --- |
| B1 | `docs-ask-chat`（U1） | `aidlc-developer-agent` |

複数の担当に分ける板は無い。根拠は `unit-of-work.md` と `stories.md`。
