# Reverse Engineering — Questions

## Question 1

このワークスペースのコード知識ベースはありますが、スコープ記録より前のもので、検証済みとして主張できる対象範囲が残っていません（判定: UNKNOWN_SCOPE。スコープ版の YAML ブロックがありません）。全体を調べ直すと 9 つの文書が置き換わります。対象を絞った調査は、既存の記述を残したまま今回の範囲を足し、再検証できない深いカバレッジは浅い扱いに下げます。

A. Full rescan — リポジトリ全体を調べ直し、9 つの文書をすべて置き換える
B. Focused scan — 今回の意図（ドキュメントの AI 質問を、回答の表示のままにせず、質問したらチャット画面へ移ってそこで繰り返し質問できるようにする）の範囲を調べて知識ベースを広げる。範囲外の既存の記述は残し、再検証できない深いカバレッジは浅い扱いに下げる
X. Other (please specify)

[Answer]: A. Full rescan
