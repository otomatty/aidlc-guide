# セキュリティの実現

新しい認証は足さない。

| ID | 実現機構 | 非該当のとき |
| --- | --- | --- |
| NFR3.1 | 会話は拡張ホストの `ExtensionContext.globalState` に置く。ワークスペースのファイルにも `workspaceState` にも書かない。読み書きは質問を処理する側が持つ | — |
| NFR3.2 | 保存の経路にクラウド通信を足さない。回答は既存のローカル CLI（`api-core` の ai-cli 隔離）のまま | — |
| NFR5.1 | ホストモードでは、既存の ask / job / cancel / evidence の拒否をそのまま使う。チャット画面は `hostMode` が真のとき送信しない | — |

入力の上限 2000 字は画面側で止める。成否は `DocsQaResult<T>` のまま。同梱ドキュメントの読取は既存の `guardPath` のまま。秘密情報を VSIX に入れない既存の衛生も変えない。
