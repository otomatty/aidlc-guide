# Functional Design — Questions

塊は `docs-ask-chat` です。画面の動き、履歴は直近 8 件、質問は 2000 字、応答中は送れない、ホストモードは拒否、保存は質問を処理する側がこのマシン上に一つ持つ、データベースは使わない、は決まっています。聞き直しません。

## Question 1

閉じたあとも残る会話を、このマシンのどこに置きますか。持ち主は質問を処理する側です。データベースは使いません。

A. 拡張が持つローカルな保存領域に置く。ワークスペースのファイルには書かない
B. このインストール用のファイルとして、ワークスペースの外に置く
X. Other (please specify)

[Answer]: A. 拡張が持つローカルな保存領域に置く。ワークスペースのファイルには書かない
