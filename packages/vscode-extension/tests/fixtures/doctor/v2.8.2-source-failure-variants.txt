AI-DLC doctor

Machine

Project (.claude, Claude Code)
  fail  Workspace source boundary binds: no (budget-entries at .: the project root lists more than 250000 entries)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-entries at packages/生成物: more than 250000 directory entries)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-entries at .: the multi-repo roof lists more than 250000 entries)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-directories at src: more than 100000 directories)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-symlinks at src/shared: more than 100000 symlinks)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-files at src: more than 250000 files)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (budget-bytes at src: more than 4294967296 bytes of source)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (source-only-budget at src/external: a symlinked external tree holds more than 50000 source files or 1073741824 bytes)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at .: the project root could not be listed: EACCES: permission denied, opendir '.')
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at .aidlc-source-paths.json: .aidlc-source-paths.json could not be read: EACCES: permission denied)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at .: the workspace root could not be resolved: ENOENT: no such file or directory)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at src/日本語.ts: the file could not be hashed)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at src: the directory could not be resolved: ENOENT: no such file or directory)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at src: the directory could not be listed: EACCES: permission denied)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at src/main.ts: the entry could not be stat'ed: EACCES: permission denied)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at src/shared: the symlink could not be read: EACCES: permission denied)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at service-a/src/shared@target: the symlink target could not be stat'ed: ENOENT: no such file or directory)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (unreadable at .: the multi-repo roof could not be listed: EACCES: permission denied)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (excluded-path at .claude/tools/main.ts: a registered source path lies under the framework shell or a hard-excluded directory)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (excluded-path at src/shared: a registered source path lies under a path this worktree excludes)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (excluded-path at .git/hooks: a registered source path resolves into the framework shell or a hard-excluded directory)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (external-symlink at service-a/src/shared: a registered source path leaves the project through a symlink and comes back inside it)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (dangling-symlink at src/shared: a registered source path resolves through a symlink whose target is missing)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (symlink-loop at src/shared: the symlink loops or its chain cannot be read)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (symlink-loop at src/shared: a registered source path could not be resolved: a symlink in it loops or cannot be read)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (harness-shell-unresolved at .: the harness shell directories could not be resolved)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (registered-sources-invalid at .aidlc-source-paths.json: .aidlc-source-paths.json must be a regular file of at most 1 MiB, not a symlink)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (registered-sources-invalid at .aidlc-source-paths.json: .aidlc-source-paths.json must be {"version":1,"paths":[<at most 10000 strings>]})
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (registered-sources-invalid at .aidlc-source-paths.json: registered source path "../日本語\"file.ts" must be a relative path inside the project without "." or ".." segments)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (registered-sources-invalid at .aidlc-source-paths.json: .aidlc-source-paths.json could not be parsed: SyntaxError: Unexpected token '}')
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (worktree-context-unresolved at .aidlc/worktree-meta.json: the worktree's source exclusion context could not be read)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at src/main.ts: the file produced no listing entry)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at src/shared: the symlink produced no listing entry)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at src/shared: the dangling symlink produced no listing entry)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at vendor/repo: the embedded git repository has no resolvable HEAD commit)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at vendor/repo: the embedded repository produced no listing entry)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at src/socket: the special file produced no listing entry)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.
  fail  Workspace source boundary binds: no (walk-failed at .: the source walk stopped without a recorded reason)
        fix: Plan Approval decisions are refused while the source cannot be bound. Shrink or exclude the offending path, declare the real source under excluded directories in .aidlc-source-paths.json, or remove the broken symlink; then re-run the fingerprint command and re-present the plan. Last resort, human only: type `Override Plan Approval: <reason>` in chat and let the conductor run answer --override.

Framework integrity

38 problems, 0 warnings.
