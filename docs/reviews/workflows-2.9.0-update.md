# aidlc-workflows 2.9.0 更新記録

対象は公式タグ `v2.9.0`、SHA `22f5d1b15a064c9ae80046e5b1761d5877e2f69f`。比較元は公式 `v2.8.2` の `355903d6dc8eb07d3c77180be5d40ed679d6a40f`。作業用の upstream checkout にある未公開変更は含めていない。

## 文書と日本語訳

- [同期前の差分一覧](official-docs-diff-2.9.0.md)の追加 2 ページ・変更 53 ページを確認した。削除候補の 3 ページは Guide 独自の導入案内・スコープ案内・更新ハイライトなので保持した。
- 英語文書、Claude / Cursor の配布ツリー、manifest を同じ公式 SHA へ同期した。
- 新しい `overview/releases/2.9.0.md` と `reference/20-commit-provenance.md` の日本語訳を追加した。既存の日本語では Classic の 18 ステージ、設定の一括変更、手続きの on/off、アーカイブ、随時の自律実行、モデル設定、配布物の区別、エンジン内部パスを更新した。
- 日英の更新ハイライトと Guide 独自の導入・スコープ案内を更新した。過去リリースの履歴原文と集約 changelog は従来どおり英語で保持し、今回の 2.9.0 は全文の日本語履歴を収録した。
- 日本語の内部リンク 924 件を画面と同じパス・見出し解決規則で検査し、14 箇所の見出しリンクを修正した。再検査で不一致は 0 件。変更したシーケンス図を含むオーケストレーター章の Mermaid 4 図も構文検証した。
- 人間の翻訳承認を表す `official-docs.translations.json` は変更していない。AI 検索が未承認訳から原文へ戻る仕様も維持する。

## 互換性

- State Version 8・基本グラフ 33 ステージを維持。ドキュメント、bridge、GUI の導入対象を 2.9.0 に揃えた。Classic の既存インテントは記録済みグラフを保持する。
- `.aidlc-engine/reviews/` と旧 `.aidlc-reviews/` の両方を読み取り、要求 ID・パス・digest の照合と、不正 JSON・symlink・hardlink の拒否を両形式で確認した。
- エンジン内部ファイルをソース指紋から除外し、レビュー記録の変更監視は維持した。Windows で保存先ディレクトリとファイルを初めて作った場合の通知も回帰検証した。
- Doctor の対応版と日本語診断を更新した。未知の説明を原文で残す扱いは維持した。
- シェル同期時のローカル Cursor ガードを保持した。Guide の UI 検証スキルが同期で消えないよう、ディレクトリ単位の保護と CI の差分許可を追加した。
- 更新・ロールバック・新しい版へのダウングレード拒否を確認した。未公開エンジン向けのカスタマイズ契約を公式対応済みとみなしていない。

## 検証

- `VITEST_MAX_WORKERS=4 bun run check`: 182 ファイル、3,203 件成功、6 件 skip。lint・整形・型・検索索引・監査 shard の検査も成功。`bun audit` は脆弱性なし。
- 公式タグへの `check-workflows-drift`: findings 0、blocking 0。
- 成果物マップ・検索索引を再生成。成果物の説明は英語／日本語とも 122 件、表の行順は 65 / 65 一致。
- `bun run package:extension`: 成功。VSIX の文書 503 ファイルを作業ツリーとバイト単位で照合し、日本語 102 ページと 2.9.0 の manifest を確認した。生成する `guides.version.json` は拡張の現行版 0.22.3 と一致する。
- ローカル Dashboard を Edge の 1280×800 で操作し、「ドキュメント → ワークフロー → 更新履歴 → 更新のハイライト → 2.9.0」の経路、日本語／英語の切り替え、新しいコミット来歴を確認した。スクリーンショットと ARIA 記録はローカルの `verify-aidlc-guide/evidence/2026-09-16T063933-104Z/` に保存し、検証用サーバーは停止した。

今回の画面検証は Dashboard SPA で実施した。VS Code / Cursor の実拡張ホストでネイティブ導入を実行する検証、利用中のエディターへのインストール、マージ・公開は行っていない。拡張の manifest の版は変更せず、PR は `release:patch` を指定する。

最新 main `962c49e` を取り込み、フックテストの競合は main 側の Bun サブプロセス方式を採用して解消した。上記の全体 check と VSIX 照合は統合後にも再実行して成功した。
