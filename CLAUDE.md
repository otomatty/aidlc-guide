# Claude Code

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. The import below gives it the
same project rules Cursor reads, so edit `AGENTS.md` rather than this file.
Project rules do not go in `.claude/CLAUDE.md` either: the aidlc-workflows
sync overwrites `.claude/` with upstream's copy.

@AGENTS.md
